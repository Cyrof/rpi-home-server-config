---
- name: k3s_power | Start cluster
  ansible.builtin.import_tasks: start.yaml
  tags:
    - start

- name: k3s_power | Shutdown cluster
  ansible.builtin.import_tasks: shutdown.yaml
  tags:
    - shutdown

- name: k3s_power | Update nodes (apt + reboot, rolling-safe)
  ansible.builtin.import_tasks: update.yaml
  tags:
    - update
